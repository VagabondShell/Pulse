services:
  database:
    image: postgres:17.9-alpine
    container_name: incident-db
    ports:
      - "5432:5432" 
    env_file:
      - .env
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:
